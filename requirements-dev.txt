# Local-only: needed to RUN prepare_data.py and the notebooks once.
# These are NOT installed on the deployed app (the app uses requirements.txt).
#
# Loose ranges on purpose: this must NOT downgrade a shared base env
# (e.g. the AlphaCast project needs numpy>=2 / scikit-learn>=1.6).
# HomLuv's code has no version-specific behaviour, so newer is fine.

streamlit>=1.39
numpy>=1.26
pandas>=2.2
scikit-learn>=1.5
Pillow>=10.0
torch>=2.3
torchvision>=0.18
jupyter>=1.0
matplotlib>=3.8
