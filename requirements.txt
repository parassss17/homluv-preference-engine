# Deployed app dependencies only (light, no PyTorch on the server).
# Ranges (not hard pins) so the host picks versions with prebuilt wheels
# for whatever Python it runs -- avoids slow/failing source builds.
streamlit>=1.39,<2.0
numpy>=1.26
pandas>=2.2,<2.3
scikit-learn>=1.5
Pillow>=10.0
